--- xpmem/kernel/xpmem_pfn.c	2021-01-19 08:27:49.372770485 -0800
+++ xpmem_patch/kernel/xpmem_pfn.c	2021-01-24 22:05:24.415715468 -0800
@@ -251,6 +251,8 @@
 	    cpu_to_node(task_cpu(current)) != cpu_to_node(task_cpu(src_task))) {
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 3, 0)
 		saved_mask = current->cpus_mask;
+#elif LINUX_VERSION_CODE >= KERNEL_VERSION(4, 18, 0)
+		saved_mask = current->cpus_mask;
 #else
 		saved_mask = current->cpus_allowed;
 #endif
